
<VirtualHost *:{{ setup.http_port }}>
    ServerName {{ logio.subdomain }}.{{ setup.domain }}
    ProxyPreserveHost On
    ProxyRequests Off
    ProxyVia Off
    ProxyPass / http://{{ vm_ip }}:{{ logio.port }}/
    ProxyPassReverse / http://{{ vm_ip }}:{{ logio.port }}/
</VirtualHost>